// Entry types for the event. `label` is what the registration form shows and
// submits (formSubmit.js strips it down to the digits); `value` is the numeric
// price actually stored in the sheet's Entrada column.
export const ENTRY_TYPES = [
  { label: 'General — $20.000', value: 20000 },
  { label: 'Personas negrxs y marronxs — $15.000', value: 15000 },
];

# Dependencies
node_modules
.pnp
.pnp.js

# Build / generated output
dist
dist-ssr
*.local
.cache
.output
build
coverage
.nyc_output

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Environment variables & secrets (keep the example file only)
.env
.env.*
!.env.example
.env*.local
.envrc
.flaskenv
*.secret
secrets*
credentials*
*.key
*.pem
*.p12
*.crt
*.der
service-account*.json
google-credentials*.json

# Platform / deployment metadata
.vercel
.netlify
.firebase
.wrangler

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~
.history

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
*.stackdump

# Windows alternate data stream (ADS) metadata — can leak download source / zone info
*Zone.Identifier*
*:$DATA

# Optional: lock files can be kept in a public repo, but if you want to avoid 
# disclosing exact dependency resolutions you can uncomment the next line:
# # package-lock.json

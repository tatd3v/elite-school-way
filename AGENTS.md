# Project Notes for AI Agents

Elite Way School — Ballroom event registration SPA. Preact + Vite + Tailwind frontend,
Google Sheets/Apps Script backend (no traditional server/DB).

## Tech stack & structure
- **Framework**: Preact (`preact/hooks`, `preact/compat` for `createPortal`), not React — imports must come from `preact/hooks` / `preact/compat`, not `react`.
- **Build**: Vite (`npm run dev`, `npm run build`, `npm run preview`).
- **Styling**: Tailwind CSS with a custom design-token config (colors like `on-surface`, `surface-container`, font sizes like `text-display-lg`, spacing like `px-margin-mobile`). `@tailwind`/`@apply` warnings from generic CSS linters in `src/index.css` are expected/harmless — Tailwind processes them at build time.
- **Routing**: `preact-router`.
- **Verification**: `npm run build` (or `npx vite build`) is the fast sanity check used throughout this project's history. Also available: `npm run lint`, `npm run test`.
- Run shell commands via `wsl -e bash -lc "cd ~/elite-school-way && ..."` — the repo is accessed through a WSL UNC path from Windows.

## Project Folder Structure (DO NOT CREATE FILES OUTSIDE THESE FOLDERS)

```
elite-school-way/
├── src/
│   ├── components/          # Preact components (.jsx)
│   │   ├── AdminDashboard.jsx
│   │   ├── AdminLogin.jsx
│   │   ├── AdminPanel.jsx
│   │   ├── BottomNavigation.jsx
│   │   ├── Categories.jsx
│   │   ├── CategoryCard.jsx
│   │   ├── CodeOfConduct.jsx
│   │   ├── DashboardHeader.jsx
│   │   ├── DesktopNavigation.jsx
│   │   ├── DressCode.jsx
│   │   ├── EventDetails.jsx
│   │   ├── FinalCTA.jsx
│   │   ├── Footer.jsx
│   │   ├── Header.jsx
│   │   ├── Hero.jsx
│   │   ├── ParticipantEditModal.jsx
│   │   ├── RegistrationModal.jsx
│   │   ├── Router.jsx
│   │   ├── RulesSection.jsx
│   │   ├── SearchBar.jsx
│   │   ├── StaffEditModal.jsx
│   │   ├── StaffManagementSection.jsx
│   │   ├── StaffMemberCard.jsx
│   │   ├── StaffSection.jsx
│   │   └── ThemeToggle.jsx
│   ├── services/            # Business logic & API calls (.js)
│   │   ├── authService.js
│   │   └── dashboardService.js (JSONP for reads, no-cors fetch for writes)
│   ├── utils/               # Helper functions (.js)
│   │   ├── auth.js
│   │   ├── driveImage.js
│   │   ├── formSubmit.js
│   │   ├── jsonp.js
│   │   └── theme.js
│   ├── data/                # Static data files (.js)
│   │   ├── categories.js
│   │   ├── countryCodes.js
│   │   ├── conductRules.js
│   │   └── dressCodes.js
│   ├── config/              # Configuration files (.js)
│   │   └── constants.js
│   ├── assets/              # Images, logos, etc.
│   │   ├── favicon.png
│   │   ├── favicon_dark_bg.png
│   │   ├── hero_background.png
│   │   ├── logo.png
│   │   ├── logo_dark_bg.png
│   │   ├── long_logo.png
│   │   └── long_logo_dark_bg.png
│   ├── App.jsx              # Main app component
│   ├── main.jsx             # Entry point
│   └── index.css            # Global styles
├── src/tests/               # Vitest test files (.test.js)
│   ├── appsScript.integration.test.js
│   └── registrations.integration.test.js
├── public/                  # Static files (copied to dist/)
├── dist/                    # Build output (generated by Vite)
├── node_modules/            # Dependencies (generated by npm)
├── .env                     # Environment variables (DO NOT COMMIT)
├── .env.example             # Example env file
├── .eslintrc.cjs            # ESLint config
├── .prettierrc.json         # Prettier config
├── .gitignore               # Git ignore rules
├── package.json             # Dependencies & scripts
├── package-lock.json        # Locked versions
├── vite.config.js           # Vite config
├── vitest.config.js         # Vitest config
├── tailwind.config.js       # Tailwind config
├── google-apps-script.md    # Backend code (Apps Script)
├── SETUP.md                 # Setup guide
├── ADMIN_SETUP.md           # Admin panel setup
├── TEST.md                  # Testing guide
├── AGENTS.md                # This file (AI agent notes)
├── README.md                # Project overview
└── ROW_INSERTION_LOGIC.md   # Technical deep-dive
```

### Key Rules for File Creation
- **Components** → `src/components/` (must be `.jsx`)
- **Services/Business Logic** → `src/services/` (must be `.js`)
- **Utilities/Helpers** → `src/utils/` (must be `.js`)
- **Static Data** → `src/data/` (must be `.js`)
- **Configuration** → `src/config/` (must be `.js`)
- **Tests** → `src/tests/` (must be `.test.js` with proper Vitest structure)
- **Test Setup/Helpers** → `src/test/` (setup.js, helpers, etc.)
- **Documentation** → Root directory (must be `.md`)
- **Assets** → `src/assets/` (images, logos, etc.)

### Important: Keep AGENTS.md in Sync
- **ALWAYS** update the folder structure diagram above whenever a new file or folder is created or deleted
- When deleting files, remove them from the structure diagram
- When adding files, add them to the appropriate section
- This keeps the AI agent's memory accurate and prevents creating files in wrong locations
- Do not rely on the user to remember — maintain this automatically

### NEVER Create Files In
- ❌ Root directory for code (except `.env`, `.eslintrc.cjs`, `.prettierrc.json`, config files, and `.md` docs)
- ❌ `src/` directly (use subdirectories)
- ❌ `node_modules/` or `dist/` (generated)
- ❌ Outside `src/` for source code

## Backend: Google Apps Script (`google-apps-script.md`)
This file is the **source of truth** for the backend script — it must be manually copy-pasted into the Google Apps Script editor (Extensions > Apps Script on the Google Sheet) and does not deploy automatically.

Key sheets (auto-created lazily on first use, not up front):
- **Registrations** — form submissions (`initializeSheet`)
- **Users** (formerly "Admins") — admin/viewer accounts (`initializeAdminsSheet`), columns: Email, Password Hash (SHA-256), Role, Name
- **Staff** — staff/faculty directory (`initializeStaffSheet`), columns: Name, Role, Bio, Photo URL, Social Links, Display Order, Is Visible

### Critical Apps Script gotchas (already hit and fixed multiple times — don't repeat)
1. **`ContentService` has NO `setHeader`/`addHeader` API.** Do not add CORS headers this way — it throws `TypeError: ... setHeader is not a function` at runtime (only surfaces when the deployed endpoint is actually hit, e.g. via `curl`, not from a code review). `createJsonResponse` must stay a plain `ContentService.createTextOutput(...).setMimeType(...)` with no header calls.
2. **CORS is handled by avoiding it entirely**, not by fixing headers:
   - Reads (`getRegistrations`, `getStaff`, `login`) go through **JSONP** (`src/utils/jsonp.js`, shared by `dashboardService.js` and `authService.js`) — loads response via a `<script>` tag, which sidesteps CORS. Apps Script's `doGet` supports a `callback` param for this.
   - Writes (form submit, staff update/delete/toggle) use `fetch` with `mode: 'no-cors'` and `Content-Type: 'text/plain'` (not `application/json`) to avoid triggering a preflight `OPTIONS` request, which Apps Script can't answer properly anyway.
   - Login (`AUTH_CONFIG.LOGIN_ACTION`) is sent as a JSONP **GET** (email/password as query params) — note this means credentials are visible in browser history/server logs; acceptable for this project's low-stakes admin demo but flagged as a real trade-off.
3. **Editing the Apps Script code and saving does NOT update the live deployment.** You must explicitly go to **Deploy → Manage deployments → (pencil/edit icon) → Version: "New version" → Deploy**. Forgetting this step is the single most common cause of "it still doesn't work" — always ask/verify this was done when debugging a script behavior change, and it can be confirmed by `curl`-ing the deployed URL directly and reading the raw HTML error (Apps Script returns error details in a simple `<div class="errorMessage">` on failure, e.g. `TypeError: ... (línea N, archivo "Code")`), which reveals the exact deployed line number — compare against the current script to check if the deployment is stale.
4. Deployment must be **Execute as: Me**, **Who has access: Anyone**.

## Roles (Users/Admins sheet)
- `admin` role → full dashboard access (actions menu, edit/delete participants, manage staff).
- Any other role value (e.g. `viewer`) → read-only dashboard (no Acciones column, no staff edit controls). Enforced both in UI (hidden) and in handlers (`if (!isAdmin) return`).

## Known fragile areas / external dependencies
- **Staff photo hosting**: Google Drive share links and Imgur album links do NOT work as direct `<img src>` — they need direct file links (`i.imgur.com/...` for Imgur; Drive requires "Anyone with the link" sharing + a direct embed format, and even then Drive hotlinking has been unreliable/rate-limited by Google). `src/utils/driveImage.js` has a best-effort multi-candidate fallback chain for Drive links, but recommend migrating to Imgur/Cloudinary/self-hosted images long-term.
- **Old Android WebView compatibility**: avoid the CSS `aspect-ratio` property (unsupported on older Android WebViews — collapses container to 0 height, making content invisible). Use the classic padding-percentage trick instead (`position: relative` + `padding-top: X%` + absolutely positioned child) — see `StaffMemberCard.jsx`.
- **Mobile horizontal overflow**: `html`/`body` in `src/index.css` have `overflow-x: hidden` as a safety net. When adding new large headings, always step down font size responsively (see `Hero.jsx`'s `text-3xl md:text-display-lg-mobile lg:text-display-lg` pattern) rather than using bare `text-display-lg`, since 56px headings can overflow narrow viewports.

## Pagination
- **AdminDashboard (Participants)**: Uses proper `currentPage`/`pageSize` pagination (state: `currentPage`, `pageSize`)
- **StaffManagementSection (Staff)**: Uses proper `currentPage`/`pageSize` pagination (state: `currentPage`, `pageSize`)
- Both use `.slice((currentPage - 1) * pageSize, currentPage * pageSize)` to display the active page
- `Filas` selector updates `pageSize` and resets to page 1
- Page number buttons, previous/next chevrons navigate `currentPage`
- Text shows: `Mostrando {start}-{end} de {total}`

## Conventions
- Spanish UI copy throughout (site is for a Spanish-speaking ballroom community in Bogotá, Colombia).
- Gender-neutral Spanish (`x` endings like "Seleccionadxs", "Listx") is used intentionally in some copy — don't "fix" this.
- New shared utilities go in `src/utils/`; new datasets go in `src/data/`.
- Don't add new npm dependencies for small self-contained needs (e.g. JSONP helper, country code list) if a simple in-repo utility suffices.

## Before Solving Anything
- Always check `AGENTS.md`, `README.md`, `SETUP.md`, `TEST.md`, and any other relevant documentation first.
- Trace the actual code path (where the feature already lives, existing patterns, related services/components).
- Use existing code patterns and utilities. Do not introduce unnecessary new files, new dependencies, or new abstractions unless the user explicitly asks.
- **Testing is Vitest only** (terminal: `npm run test`). Do NOT add console-based testing utilities or browser test runners.
- **Do NOT import test utilities in components** — tests run independently via Vitest, not in the browser.

## User-Specified Guidelines (CRITICAL)
- **ONLY modify what the user explicitly mentions.** Do NOT make proactive changes to other components or features.
- **Always clarify mobile vs. desktop changes.** If uncertain whether a change is for mobile or desktop, ASK THE USER before proceeding.
- **Always ask permission before modifying other components.** If you want to proactively change another component (e.g., StaffManagementSection when working on AdminDashboard), ask the user first for approval.
- **Scope creep prevention:** Stay focused on the exact task requested. Avoid "while I'm at it" changes unless explicitly approved.

## Code Quality & Best Practices (Always Apply)
- **DRY (Don't Repeat Yourself)**: Extract repeated logic into reusable functions/utilities. Avoid duplicating code across components or services.
- **KISS (Keep It Simple, Stupid)**: Prefer simple, straightforward solutions over complex ones. Avoid over-engineering.
- **Naming**: Use clear, descriptive names for variables, functions, and components. Avoid abbreviations unless they're standard (e.g., `id`, `url`).
- **Error Handling**: Always handle errors gracefully. Log meaningful error messages for debugging. Don't silently fail.
- **Comments**: Add comments for "why", not "what". The code should be clear enough that "what" is obvious. Comment complex logic, edge cases, and non-obvious decisions.
- **Testing**: When fixing bugs or adding features, write tests first (TDD) if possible. Verify fixes work and don't break existing functionality.
- **Performance**: Avoid unnecessary re-renders in Preact. Use memoization, proper dependency arrays, and lazy loading where appropriate.
- **Accessibility**: Ensure UI is keyboard-navigable and screen-reader friendly. Use semantic HTML. Test on mobile.
- **Documentation**: Keep this file (`AGENTS.md`) updated with new patterns, gotchas, or important decisions. Document non-obvious setup steps.

## SOLID Principles (Always Apply)
**S - Single Responsibility Principle**: Each class/function/component has ONE reason to change. If it does multiple things, split it.
  - ✅ `dashboardService.js` handles data fetching only
  - ✅ `AdminDashboard.jsx` handles UI state and layout only
  - ✅ `staffTestRunner.js` handles testing only
  - ❌ Don't mix business logic with UI rendering

**O - Open/Closed Principle**: Code should be open for extension, closed for modification. Use composition and abstraction.
  - ✅ Add new staff actions by extending `STAFF_CONFIG.ACTIONS`
  - ✅ Create new components by composing existing ones
  - ❌ Don't modify existing functions to add new features; extend them instead

**L - Liskov Substitution Principle**: Subtypes must be substitutable for their base types. Derived classes should not break the contract.
  - ✅ All staff members follow the same shape: `{ rowIndex, name, role, bio, photo, socialLinks, displayOrder, isVisible }`
  - ✅ All API responses follow consistent structure: `{ status, data, error }`
  - ❌ Don't return different shapes for the same function

**I - Interface Segregation Principle**: Clients should not depend on interfaces they don't use. Keep interfaces small and focused.
  - ✅ `dashboardService` exports only the methods needed by components
  - ✅ Test utilities (`staffTests`) expose only the functions users need
  - ❌ Don't force components to import/use functions they don't need

**D - Dependency Inversion Principle**: Depend on abstractions, not concrete implementations. Inject dependencies.
  - ✅ Components depend on `dashboardService` (abstraction), not directly on fetch/JSONP
  - ✅ Tests can mock `dashboardService` for unit testing
  - ❌ Don't hardcode API calls directly in components

## Conversation Memory & Token Efficiency
- Before asking clarifying questions or re-asking for information, review the existing conversation context (chat memory) and this `AGENTS.md` file.
- Before creating, moving, or referencing files, always check the **Project Folder Structure** diagram above and the current `src/` layout.
- Avoid redundant actions: don't re-read files that were just read, and don't re-run commands (builds, tests, searches) whose outputs are already known.
- Prefer targeted `read`/`grep`/`code_search` over re-explaining the codebase to answer follow-ups.

## CRITICAL: DO NOT MODIFY WORKING STYLES OR LAYOUTS
- **NEVER change component styles, layouts, or HTML structure without explicit user permission**
- **NEVER refactor working code "to improve it" - only fix bugs or implement requested features**
- **If a component is working and styled correctly, LEAVE IT ALONE**
- **Always ask before making ANY changes to existing styles, classes, or component structure**
- **The user will explicitly tell you what to change - do ONLY that, nothing more**
- **Breaking existing good code is worse than leaving it as-is**
